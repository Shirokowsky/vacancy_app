<%= form_for @skill do |f| %>
    <% if @skill.errors.any? %>
        <%= content_for :footer do %>
        <div id="error_explanation">
          <h2><%= pluralize(@skill.errors.count, 'error')%> prohibited this emp from being saved</h2>
          <ul>
            <%= @skill.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
        <% end %>
    <% end %>
    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name, placeholder: 'skill' %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>